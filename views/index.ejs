<!DOCTYPE html>
<html class="no-js">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>SocketIOServer</title>
        <meta name="description" content="Example socket io server">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="stylesheet" href="css/core.css">

        <style>

            input:focus,
            select:focus,
            textarea:focus,
            button:focus {
                outline: none !important;
            }
            body {
                background-color: #f2f2f2;
            }

            #allPosts {
                resize: none;
                font-size: 12px;
                border-radius: 5px;
                border: #ccc solid 1px;
                line-height: 14px;
                height:100px;
                overflow: scroll;
            }

            p{
                margin:0;
                padding:0;
            }

            .jumbotron {
                background-color: rgba(6, 20, 69, 0.84);
                color: #ffffff;
            }

            #send {
                margin: 10px 0px;
            }

        </style>
    </head>
    <body>

    <!--[if lt IE 8]>
        <p class="browsehappy">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
    <![endif]-->

    <div class="container">
        <div class="jumbotron">
            <h1>E-Reader Command Server</h1>
               <p>Dispatches notifications and syncs uploads with all connected e-reader clients.</p>
        </div>
    </div>

    <div class="container">
        <div class="panel panel-default">
            <div class="panel-body">
                <h4>Notifications<button id="clearAllPosts" type="button" class="btn btn-danger btn-xs pull-right">clear</button></h4>
                <div class="form-group">
                    <div id="allPosts" class="form-control"></div>
                </div>
                <div class="form-group">
                    <input id="blast" type="text" class="form-control" placeholder="Enter notification to dispatch">
                    <button id="send" class="btn btn-success" type="button">Send</button>
                </div>
            </div>
        </div>
    </div>


    <div class="container">
        <div class="panel panel-default">
            <div class="panel-body">
                <h4>Uploads</h4>
                <form action="/upload" method="post" enctype="multipart/form-data">

                    <div class="form-group">
                        <label class="form-control" for="fileToUpload">Select file to upload</label>

                        <input type="file" name="fileToUpload" id="fileToUpload">
                    </div>

                    <button type="submit" value="Upload File" name="submit" class="btn btn-success">Upload</button>
                </form>
            </div>
        </div>
    </div>

    <!-- jQuery -->
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
    <script>window.jQuery || document.write('<script src="public/bower_components/jQuery/jquery.min.js"><\/script>')</script>

    <!-- socket.io -->
    <script src="/socket.io/socket.io.js"></script>

    <!-- APP CODE -->
    <script src="js/app.min.js"></script>
    </body>
</html>
